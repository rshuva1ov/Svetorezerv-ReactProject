import { observer } from 'mobx-react-lite';
import React from 'react';

const PostsForm = observer(() => {
    return (
        <div>
            hi
        </div>
    );
});

export default PostsForm;